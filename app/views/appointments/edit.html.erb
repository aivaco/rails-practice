<h1>Editing Appointment</h1>

<%= form_for @appointment, url: {action: "update"}, html: {class: "form-group"} do |f| %>
  <%= f.label "Select a user:", class: 'col-form-label'%>
  <%= f.collection_select(:user_id, User.all, :id,:email, {},  {:class=>"form-control"}) %>
  <%= f.label "Select appointment's day:", class: 'col-form-label'%>
  <%= f.date_field :date , class: "form-control"%>
  <br>
  <%= f.submit "Edit", class: "btn btn-btn-primary" %>
<% end %>

<%= link_to 'Back', appointments_path %>

<%= link_to 'Show', @appointment %> |
<%= link_to 'Back', appointments_path %>
